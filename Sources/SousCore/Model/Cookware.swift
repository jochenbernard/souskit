/// A piece of cookware annotated in a step with the `#...#` sigils.
public struct Cookware: Equatable, Hashable, Sendable {
    /// The cookware's name, captured verbatim.
    public var name: String
}
